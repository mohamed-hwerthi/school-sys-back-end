spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/school_dev_db
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:med}
    password: ${DB_PASSWORD:med}

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/school_dev_db
    user: ${DB_USERNAME:med}
    password: ${DB_PASSWORD:med}
    # dev: schema + demo seed (db/seed). Prod uses db/migration only.
    locations: classpath:db/migration,classpath:db/seed
    schemas: public
    baseline-on-migrate: true

  mail:
    enabled: false
    host: localhost
    port: 1025
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

logging:
  file:
    name: logs/backend.log
  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n%ex"
  logback:
    rollingpolicy:
      file-name-pattern: logs/backend-%d{yyyy-MM-dd}.%i.log
      max-file-size: 50MB
      max-history: 14
      total-size-cap: 1GB
      clean-history-on-start: false
  level:
    root: INFO
    com.schoolSys.schooolSys: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.flywaydb: DEBUG

server:
  error:
    include-stacktrace: always

# JWT — dev-only default secret (DO NOT use in production)
app:
  jwt:
    secret: ${JWT_SECRET:aVeryLongSecretKeyThatIsAtLeast256BitsForHS256Algorithm2026!}
  cors:
    allowed-origins: "*"
  tenant:
    # A new school starts EMPTY: schema/tables only, no demo data.
    # (Add classpath:db/tenant-seed back to pre-fill demo data if needed.)
    flyway-locations: classpath:db/tenant-migration

# Actuator — expose everything in dev
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
