server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # SPA — redirect all routes to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy API calls to backend
    location /api/ {
        proxy_pass http://backend:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 10M;
    }

    # Proxy uploads (served by Spring Boot)
    location /uploads/ {
        proxy_pass http://backend:8080/uploads/;
        proxy_set_header Host $host;
        expires 1d;
        add_header Cache-Control "public";
    }

    # Proxy Swagger
    location /swagger-ui/ {
        proxy_pass http://backend:8080/swagger-ui/;
    }

    location /v3/api-docs {
        proxy_pass http://backend:8080/v3/api-docs;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    gzip_min_length 256;
}

# Subdomain routing for vitrine sites: *.schoolsys.com -> /vitrine/$subdomain
# Uncomment when wildcard DNS is configured
# server {
#     listen 80;
#     server_name ~^(?<school>.+)\.schoolsys\.com$;
#     root /usr/share/nginx/html;
#     index index.html;
#
#     # Rewrite root to vitrine path — the React SPA handles the rest
#     location / {
#         try_files $uri $uri/ /index.html;
#         # The frontend detects subdomain and loads /vitrine/$school
#     }
#
#     location /api/ {
#         proxy_pass http://backend:8080/api/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         client_max_body_size 10M;
#     }
#
#     location /uploads/ {
#         proxy_pass http://backend:8080/uploads/;
#         expires 1d;
#     }
#
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
#
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
#     gzip_min_length 256;
# }
